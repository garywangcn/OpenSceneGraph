SET(TARGET_SRC ReaderWriterS3C.cpp )
SET(TARGET_LIBRARIES_VARS S3CLIB_LIBRARY )
#### end var setup  ###
SETUP_PLUGIN(s3c)

IF(MSVC)
    SET_TARGET_PROPERTIES("${TARGET_DEFAULT_PREFIX}${TARGET_NAME}" PROPERTIES LINK_FLAGS_DEBUG "/NODEFAULTLIB:MSVCRT")
ENDIF()
